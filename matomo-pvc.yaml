apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matomo-data-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: csi-cinder-classic
  resources:
    requests:
      storage: 2Gi

